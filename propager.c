#include<stdio.h>
#include<stdlib.h>
/* Ordre de traitement G*/
int propager(image g, int tab, int num_tache){
	int i;
	g
	for(i=0;i<4;i++){
		if(tab[y*w+x]==0] && compar()){
			tab[x+w*y]=num_tache;
		}
	}

}