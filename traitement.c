#include<stdlib.h>
#include<stdio.h>
/*dans le traitement je retranscrit l'image dans le tableau d'int avec une meme valeur pour tout les points d'une meme tache*/
void traitement(image g){
	int i;
	int num_tache=0;
	int tab [image.w*image.h];
	for(i=0;i<(image.w)*(image.h);++i){
		if(tab[i]!=0){
			propager()/*a faire*/ 
		}
		num_tache++;
	}
}
